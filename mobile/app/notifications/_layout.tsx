import React from 'react';
import { Slot } from 'expo-router';

// Notifications layout - wraps notifications routes and renders child pages
export default function NotificationsLayout() {
  return <Slot />;
}
